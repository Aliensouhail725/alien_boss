{
  "framework": "flask",
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "echo 'No build required for Flask'",
  "outputDirectory": ".",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "env": {
    "FLASK_ENV": "production"
  }
}